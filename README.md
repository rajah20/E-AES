The aes_rns_metrics_test.py computes all the metrics when the code is run, there are two buttons that allows you to computer for either plain text sensivivty of key sensitivity. The aes_encrypt.py simply encrypts the plain text when an AES key is provided, the x value selecetd and the plain text to be encrypted is also provided. The aes_decrypt.py on the other hand decrypts the 32 bit encrypted ciphertext, when the AES key is provided, it will cycle through all x values (1,...,125) to get the x value that was used for the encryption.
